export interface NotionPage {
  title: string;
  description: string;
  videoUrl: string;
  thumbnailUrl: string;
  publishedAt: string;
}

export interface NotionResponse {
  id: string;
  url: string;
}
